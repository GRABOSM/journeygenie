/**
 * CRA dev server proxy (optional).
 * Grab Maps calls use REACT_APP_GRAB_MAPS_API_URL + Bearer from the browser or your gateway.
 * Street-view / OpenStreetCam proxies were removed with that feature.
 */
module.exports = function setupProxy() {
  // Intentionally empty.
};
